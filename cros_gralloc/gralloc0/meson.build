# gralloc0
shared_library(
    'gralloc.minigbm_gd',
    files('gralloc0.cc'),
    link_with : libminigbm_gralloc,
    name_prefix : '',
    dependencies : deps,
    install : true,
    install_dir : get_option('libdir') / 'hw',
)
